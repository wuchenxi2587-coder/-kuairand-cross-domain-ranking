# === Core (DIN Baseline + EDA 共用) ===
pandas>=1.5.0
numpy>=1.23.0
tqdm>=4.64.0

# === DIN Baseline Pipeline ===
pyarrow>=10.0.0
pyyaml>=6.0
psutil>=5.9.0

# === EDA 分析 ===
matplotlib>=3.6
duckdb>=0.9
# polars>=0.20  # 可选，大文件分析加速

